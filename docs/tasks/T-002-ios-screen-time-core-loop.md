# T-002 — iOS: Screen Time core loop on James's iPhone

**Goal:** an iPhone app that asks for Screen Time access once, hides Instagram and TikTok by bundle identifier when James taps "Start class mode," and brings them back automatically at a scheduled end time — without a server.

**Why:** this is the riskiest mechanism in the product and the one Apple constrains most. If bundle-identifier hiding works on a real phone today, the whole engine design holds. Proving it first, with nothing else attached, is the cheapest possible test. Read `docs/ios.md` before starting.

**Scope — in:** the `TapIn` app target + the `TapInMonitor` DeviceActivityMonitor extension, generated by XcodeGen; authorization; hide/unhide by bundle id; scheduled unhide via DeviceActivity; a minimal SwiftUI screen. **Out:** NFC, VPN (T-003), networking, onboarding screens, the demo's visual design (a plain screen is fine here).

## Steps
1. Create `apps/ios/project.yml` (XcodeGen) with two targets — see `docs/ios.md` for bundle ids, capabilities and entitlements. Deployment target iOS 17.0. Automatic signing with James's team (`DEVELOPMENT_TEAM` from `xcodebuild -showBuildSettings` after he signs into Xcode, or leave for Xcode to fill on first open — report which). Entitlement files checked into git. App Group `group.com.tapinschools.tapin` on both targets.
2. `xcodegen generate`; open the project once so Xcode registers the App ID and capabilities (James may need to click "Try again" under Signing & Capabilities — put that in the physical steps).
3. App screen (SwiftUI, plain): authorization status line; **Allow Screen Time** button → `AuthorizationCenter.shared.requestAuthorization(for: .individual)`; **Start class mode (15 min)** button; **End class mode now** button; a countdown label.
4. `ClassMode` service (shared code, App Group aware):
   - `apply(bundleIds:)` → `ManagedSettingsStore(named: .init("classMode")).application.blockedApplications = Set(ids.map(Application.init(bundleIdentifier:)))`
   - `clear()` → `blockedApplications = nil`
   - bundle ids for this test: `com.burbn.instagram`, `com.zhiliaoapp.musically`, `com.toyopagroup.picaboo` (Snapchat), `com.google.ios.youtube`. Put the list in one file with a `// DEVELOPMENT ONLY — served by the policy API from T-007` header.
5. Scheduling: on "Start class mode," call `apply` immediately **and** `DeviceActivityCenter().startMonitoring(.classMode, during: DeviceActivitySchedule(intervalStart: now, intervalEnd: now + 15 min, repeats: false))`. In `TapInMonitor`, subclass `DeviceActivityMonitor`: `intervalDidStart` → `apply` (idempotent), `intervalDidEnd` → `clear()` + `stopMonitoring`. Verify the minimum interval rule in Apple's docs and note it in the report.
6. Stretch (only if 1–5 pass): add a second screen with `FamilyActivityPicker` and shield the selection via `store.shield.applications` — the fallback path — so we know both mechanisms work on this phone and iOS version. Label it clearly as the fallback.
7. Build for James's device from the terminal (`xcodebuild ... -destination 'platform=iOS,name=…'`) and install (`xcrun devicectl`), or hand off to Xcode's Run button — write whichever worked into `docs/local-development.md`.
8. Session-end ritual: report, `docs/PROGRESS.md`, branch `task/T-002-ios-screen-time-core-loop`, PR.

## Finish line
On James's iPhone: tap Allow → Face ID → status "approved"; tap Start → Instagram and TikTok disappear from the Home Screen and App Library within seconds; wait 15 minutes with the app closed → they return on their own; "End class mode now" also returns them immediately.

## Physical test (James) — write these into the report, step by step
1. Install Instagram and TikTok on your iPhone if they aren't there (any account, or none).
2. Run the app from Xcode on your phone. If Xcode shows a signing error, open the TapIn target → Signing & Capabilities → click "Try again" / enable the capability it names.
3. Tap **Allow Screen Time** → approve with Face ID. Screenshot the status line.
4. Tap **Start class mode (15 min)**. Go to the Home Screen: Instagram and TikTok should be gone. Search for them (swipe down) — they shouldn't appear. Screenshot.
5. Close the app. Wait 15 minutes. Check the Home Screen: both apps back. Note where they landed (same spot or end of the last page). Screenshot.
6. Tap Start again, then **End class mode now** — apps back immediately.
7. Settings → Screen Time → scroll to the Tap in entry → turn access off. Re-open the app: the status line should say "denied/not determined" (this is the revocation the server will alert on later).
8. Send the screenshots and what you saw to the tech-lead chat. Record a 30-second video of steps 4–5 for the plan.

## Report

**Date:** Aug 25, 2026

### Files created
```
apps/ios/
├── .gitignore                           # Ignores *.xcodeproj (generated)
├── project.yml                          # XcodeGen config
├── Entitlements/
│   ├── TapIn.entitlements              # Family Controls + App Groups
│   └── TapInMonitor.entitlements       # Family Controls + App Groups
└── Sources/
    ├── Shared/
    │   ├── BlockedApps.swift           # Dev-only bundle ID list
    │   └── ClassMode.swift             # ManagedSettingsStore wrapper
    ├── TapIn/
    │   ├── Info.plist
    │   ├── TapInApp.swift              # @main entry
    │   ├── ContentView.swift           # Test UI
    │   └── ClassModeViewModel.swift    # Auth + scheduling logic
    └── TapInMonitor/
        ├── Info.plist                  # NSExtensionPrincipalClass
        └── TapInMonitor.swift          # DeviceActivityMonitor subclass
```

### Build commands
```bash
# Generate Xcode project
cd apps/ios && xcodegen generate

# Build for device (after signing is set up)
xcodebuild -scheme TapIn -destination 'platform=iOS,name=iPhone (3)' -configuration Debug build

# Install to device
xcrun devicectl device install app \
  --device 355F9006-83E7-50C4-B3C4-397121D6CD29 \
  ~/Library/Developer/Xcode/DerivedData/TapIn-*/Build/Products/Debug-iphoneos/TapIn.app
```

### Minimum interval (from Apple docs)
**15 minutes minimum, 1 week maximum.** DeviceActivitySchedule requires at least 15 minutes between `intervalStart` and `intervalEnd`. This is a hard constraint in the Screen Time framework. Source: [DeviceActivitySchedule documentation](https://developer.apple.com/documentation/deviceactivity/deviceactivityschedule) and developer forum discussions.

### Signing setup
`DEVELOPMENT_TEAM` is left empty in project.yml — Xcode fills it on first open when James selects his team. James will need to:
1. Open `TapIn.xcodeproj` in Xcode
2. Go to TapIn target → Signing & Capabilities
3. Select his development team
4. If Family Controls shows an error, click "Try again" to register the capability

### Stretch goal (FamilyActivityPicker fallback)
**Not implemented yet** — pending physical test of steps 1-5. If bundle-ID hiding works, I'll add the fallback screen in a follow-up commit.

### Physical test steps (James)

**Before you start:**
- Install Instagram, TikTok, Snapchat, and YouTube on your iPhone if not already installed

**Setup:**
1. Open Terminal, run: `cd ~/code/tapin/apps/ios && xcodegen generate`
2. Run: `open TapIn.xcodeproj`
3. In Xcode: select the TapIn target → Signing & Capabilities → select your team
4. If you see "Family Controls" with an error, click "Try again"
5. Connect your iPhone, select it as the run destination
6. Press Run (⌘R)

**Test:**
1. App launches → tap **Allow Screen Time** → approve with Face ID
2. Verify status shows "Approved" (screenshot)
3. Tap **Start class mode (15 min)**
4. Press Home → Instagram, TikTok, Snapchat, YouTube should be gone
5. Search (swipe down) — they shouldn't appear (screenshot)
6. **Close the app completely** (swipe up from app switcher)
7. Wait 15 minutes
8. Check Home Screen — apps should return automatically (screenshot, note position)
9. Re-open Tap in → tap **Start class mode** again
10. Tap **End class mode now** → apps return immediately
11. Go to Settings → Screen Time → scroll to "Tap in" → turn access off
12. Re-open Tap in → status should show "Denied" or "Not requested"

**Send to tech-lead chat:**
- Screenshots from steps 2, 5, 8
- 30-second video showing apps disappearing and returning
- Any unexpected behavior

### Questions for tech lead
None at this time. Code matches `docs/ios.md` patterns exactly.

### Risks noted
- **App position after unblock:** Apple docs warn that hidden apps may return to a different Home Screen position. James should note where they land.
- **Signing:** First-time setup requires manual capability registration in Xcode. Documented in physical test steps.
