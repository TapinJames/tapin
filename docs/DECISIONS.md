# DECISIONS — dated, plain English

One line per decision: what, why, what was rejected. Newest at the bottom. James makes product decisions; the tech-lead chat makes architecture decisions; Claude Code adds a line for any implementation choice that wasn't in its brief.

| ID | Date | Decision | Why | Rejected |
|---|---|---|---|---|
| D-001 | 2026-08-25 | Fresh-start build; nothing carried over from the old TAPN iOS app. | Old code was a team prototype; the new design (two-layer engine, Supabase, dashboards on real data) is different enough that porting costs more than it saves. | Reusing the old app |
| D-002 | 2026-08-25 | iOS first, then dashboards, then Android. | iOS carries the two risks that can kill the November pilot (Screen Time on teen accounts; App Review on hiding). Prove those before spending on anything else. | Backend or dashboards first |
| D-003 | 2026-08-25 | Messages and Phone are never blocked. | Safety: parents and students must always be reachable. Apple's Always Allowed list protects them anyway. | Blocking Messages (the old prototype did) |
| D-004 | 2026-08-25 | Blocking policy is set centrally by the principal from the backend; students never choose what is blocked. | Product requirement from James; enforcement can't depend on a 14-year-old's choices. | Student-picked app lists (Apple's picker as the primary mechanism) |
| D-005 | 2026-08-25 | Two-layer engine: Screen Time hides Blocked apps by bundle identifier; an on-device VPN filters DNS against a server-controlled domain list. | Bundle-ID hiding is the only picker-free per-app mechanism Apple offers (Apple engineer example exists) but App Review has rejected it before; the VPN needs no special approval and works on Android too. Together, bypass takes two deliberate acts, both visible to the teacher. | Screen Time only; VPN only; picker-based shields as primary (kept as fallback) |
| D-006 | 2026-08-25 | Dashboard policy model: Blocked (our maintained catalog by category, all on) and Approved columns; unknown apps allowed in v1. | Apple's bundle-ID API is a block list, not an allow list. The catalog approximates an allow list without touching banking/transit/medical apps. | True allow-list ("nothing but approved") — available later via the picker path or hard-lock mode |
| D-007 | 2026-08-25 | One repository (`tapin`) with folders per app; one PROGRESS file; one task list. | Solo operator; one place for the tech-lead chat to pull; server + app changes land in one commit. | Four repos |
| D-008 | 2026-08-25 | GitHub is the middleman: Claude Code pushes branches + PRs; the tech-lead chat reviews from GitHub; James merges. | James can't review code; the PR is the unit the tech lead can see. | Pasting code between chats |
| D-009 | 2026-08-25 | Supabase (Postgres + RLS + Auth + Edge Functions) as the backend; Next.js on Vercel for dashboards. | Proven, hosted, free for the pilot; permission rules live in the database; Claude Code's strongest ground. | Custom Node API + AWS (the old prototype's path) |
| D-010 | 2026-08-25 | Rostering behind a `RosterProvider` abstraction; class code / CSV first, Clever second, SIS write-back phase two. | The pilot must not depend on Clever; Cherry Hill runs Genesis. | Building Clever first |
| D-011 | 2026-08-25 | Passwords stay in Google Drive "8. Credentials" for now; 1Password is a later task. | James's call. | 1Password from week one (tech lead's recommendation, still open) |
| D-012 | 2026-08-25 | Apple Developer Program: James's existing paid membership now; organization account after incorporation. | Family Controls and Network Extensions need a paid membership; the organization account needs the company to exist. | Waiting for incorporation |
| D-013 | 2026-08-25 | No UI mockups before build; the `_v05` demos are the spec; new iOS onboarding screens are specified in words in the task brief and built directly in SwiftUI. | Mockups would be a translation step; SwiftUI on the real phone is the faster loop. | Claude Design mockups first (reserved for marketing screenshots later) |
| D-014 | 2026-08-25 | Bypass detection: the VPN extension reports the instant it is stopped; Screen Time revocation is checked at every tap and on a silent push at each period start. | iOS does not allow timer-based background heartbeats; "missing heartbeat" would false-alarm. | Heartbeat every few minutes |
| D-015 | 2026-08-25 | CI web/supabase jobs commented out until code exists; will re-enable when apps/web/package.json and supabase/config.toml land. | `hashFiles()` at job-level `if:` fails before checkout (no workspace context). | Path filters on workflow triggers (more complex, same result) |
| D-016 | 2026-08-25 | iOS delivers no live event when a user disables Family Controls authorization (`authorizationStatus` only updates under a debugger — confirmed Apple limitation). Screen Time is enforcement only; the VPN is the live compliance sensor. Screen Time authorization is re-confirmed at each tap and app-foreground, not monitored live. | iOS provides no callback for Screen Time revocation; polling on app foreground is the only option. | Live monitoring of Screen Time status (not possible on iOS) |
