# Security

Report a vulnerability: james@tapinschools.com (subject "Tap in security"). We respond within two business days.

Never commit: secrets, keys, certificates, `.env` files, student data, database dumps. CI blocks the obvious ones; people block the rest.

Architecture and controls: `docs/security.md`. Privacy model: `docs/privacy.md`.
